class Experience {
  Experience.name(this.startDate, this.endDate, this.description, this.jobTitle,
      this.placeOfWork);

  String startDate;
  String endDate;
  String description;
  String jobTitle;
  String placeOfWork;
}